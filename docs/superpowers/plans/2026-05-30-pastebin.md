# Pastebin — Plan

**Goal:** App web en Go para pegar texto y compartirlo por un enlace único.
**Tech:** Go (net/http, html/template, go:embed, crypto/rand, sync). Sin deps externas.
Módulo: `github.com/samuelcatalanz123/pastebin-go`. Commits autoría Samuel.

## Tareas
1. **store (con prueba):** `internal/store/store.go` (Store, New, Save→code, Get, código base62) + `store_test.go` (guardar+recuperar; inexistente).
2. **web:** `internal/web/handler.go` (GET /, POST /create, GET /p/{code}) + `templates/home.html` + `templates/view.html` + `static/style.css`.
3. **main.go:** servidor (:8080/PORT).
4. **README + .gitignore.**
5. Verificar build/vet/test; arrancar y probar (crear + ver); commits.

Self-review: cobertura del spec ✔ · sin placeholders ✔ · tipos consistentes
(Store, New, Save, Get) ✔.
